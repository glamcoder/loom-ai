# Roadmap

The full, maintained roadmap lives in [docs/roadmap.md](docs/roadmap.md).

In short:

- **v0.1** — deterministic artifact compiler _(current)_
- **v0.x** — docs / examples / CI hardening
- **v1** — `llm.complete` with a `MockLlmProvider` first
- **v2** — parse / validate helpers for model output
- **v3** — workflow control (retries, checks, conditions)
- **v4** — packages / ecosystem

This is directional and may change based on user feedback.
